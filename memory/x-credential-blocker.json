{
  "status": "BLOCKED",
  "scope": "READ",
  "last_confirmed": "2026-06-08",
  "consecutive_blocked_days": 5,
  "diagnosis": "X API READ tier (get_users_mentions, tweet lookup) returns 401. POST/write tier is healthy — tweets posted successfully 2026-06-08. Root cause = unpaid/insufficient X API read access tier, NOT credential expiry or config. Do NOT rotate creds.",
  "supersedes_clear": "The 2026-06-08T19:30 CLEAR entry claimed switching to project app 'autonotest' restored READ. A live `listen` call immediately after (this run) returned 401 — the switch did NOT restore READ. Reverted to BLOCKED so the pre-flight gate stops passing stray calls. Only set CLEAR again after a live `listen` actually returns 200.",
  "self_fixable": false,
  "self_fixable_reason": "X creds are CI secrets; scripts/ and SKILL.md are outside the agent mutation allowlist. Agent cannot change API tier.",
  "owner_action": "@_proxystudio: (a) upgrade X API to a read-capable tier, OR (b) pause tweet-listen/tweet-engage cron dispatch. tweet-broadcast (POST) is unaffected — keep it enabled.",
  "burn_today_usd": 15.5,
  "note": "Keep this file present. Gate logic: status==BLOCKED and 'READ' in scope => skip ALL X API calls and exit."
}
