{
  "blocker": "x_read_endpoints_401",
  "status": "BLOCKED",
  "scope": "READ endpoints only (listen/like/engagement). POST/write is HEALTHY.",
  "first_seen": "2026-06-07",
  "last_confirmed": "2026-06-08",
  "consecutive_blocked_days": 3,
  "confirmations_today": 42,
  "wasted_api_calls_today": 70,
  "symptom": "X API v2 HTTP 401 on get_users_mentions / get_tweet / like; create_tweet (POST) returns 200 OK",
  "creds_present": true,
  "cred_lengths": {
    "TWITTER_API_KEY": 25,
    "TWITTER_API_SECRET": 50,
    "TWITTER_ACCESS_TOKEN": 50,
    "TWITTER_ACCESS_SECRET": 45
  },
  "post_endpoint_evidence": "2026-06-08: posted 2 tweets with these exact OAuth 1.0a creds \u2014 ids 2063883588840419781 (on-chain-report) and 2063883625876131850 (ecosystem-commentary). Write OAuth is valid and working.",
  "diagnosis": "CORRECTED 2026-06-08: creds are NOT invalid/expired/revoked \u2014 POST succeeds with them, refuting the prior 'invalid/expired' diagnosis. Read endpoints returning 401 while writes return 200 is the signature of an X API ACCESS-TIER limitation: the app tier permits posting but not mention/tweet-lookup reads (those require a paid read tier). Auth-layer, not rate-limit (not 429).",
  "self_fixable": false,
  "reason_not_self_fixable": "API access tier is an X developer-portal setting, not in repo; scripts/ + SKILL.md off allowlist so the pre-call gate cannot be added from my mutation surface.",
  "owner": "@_proxystudio",
  "required_action": "DO NOT rotate creds \u2014 they work for posting and rotation will not restore reads. Either (a) upgrade the X API app to a tier that allows mention/lookup reads, or (b) permanently disable tweet-listen/engagement dispatch and keep only tweet-broadcast (posting), which is healthy. Until one of those: pause tweet-listen dispatch \u2014 each run burns the day's top inference line for zero signal.",
  "guidance_for_future_runs": "Read this file FIRST, before any X API READ call (listen/like/engagement). If last_confirmed == today and status == BLOCKED, do NOT call X READ endpoints \u2014 log one line and exit. POSTING (tweet-broadcast) is NOT blocked and should run normally.",
  "estimated_wasted_inference_today_usd": 11.9
}